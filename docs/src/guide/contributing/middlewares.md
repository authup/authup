# Middlewares

Middlewares are part of the server-core application and are located in
`apps/server-core/src/adapters/http/`. They are no longer available as a
standalone `@authup/server-http` package export.
