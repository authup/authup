import{_ as e,o as a,c as i,a0 as t}from"./chunks/framework.BFac62L4.js";const c=JSON.parse('{"title":"Worker","description":"","frontmatter":{},"headers":[],"relativePath":"guide/deployment/worker.md","filePath":"guide/deployment/worker.md"}'),n={name:"guide/deployment/worker.md"};function h(o,s,r,l,p,d){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="worker" tabindex="-1">Worker <a class="header-anchor" href="#worker" aria-label="Permalink to &quot;Worker&quot;">​</a></h1><p><code>authup start</code> normally runs everything in one process: <code>server-core</code> answers the API, the console services serve their pages on the same listener, and a set of cron sweeps runs in the background. Those sweeps can be moved into a separate process, the <strong>worker</strong>, so the API replicas only serve requests. The consoles can be moved out the same way, an API set running <code>start core</code> next to a console set running <code>start console</code>; see <a href="./console-replicas.html">Console Replicas</a>.</p><p>The worker is the same image and the same binary, started in a different role:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">authup</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> start</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> worker</span></span></code></pre></div><p>In a container the command is <code>start worker</code>; on bare metal it is a second <code>authup</code> process next to the one running <code>start</code>. Without the role the API runs the worker alongside itself.</p><h2 id="what-it-runs" tabindex="-1">What it runs <a class="header-anchor" href="#what-it-runs" aria-label="Permalink to &quot;What it runs&quot;">​</a></h2><p>The worker process boots the background sweeps and the modules they stand on, and nothing else. There is no HTTP listener, so it serves no request and opens no port.</p><p>Two sweeps run today, both once a minute:</p><table tabindex="0"><thead><tr><th>Sweep</th><th>What it removes</th></tr></thead><tbody><tr><td>OAuth2 cleaner</td><td>Expired rows from <code>auth_sessions</code> and <code>auth_session_tokens</code></td></tr><tr><td>Event cleaner</td><td>Expired rows from <code>auth_events</code>, per the retention settings</td></tr></tbody></table><p>The event cleaner is only scheduled while the audit log is on and at least one applicable retention window is greater than zero; the entity-event window only counts when <code>EVENT_LOG_ENTITY_ENABLED</code> is true as well. A deployment with <code>EVENT_LOG_ENABLED=false</code> runs the OAuth2 cleaner alone.</p><p>Both delete in bounded batches and stop when another process already removed the rows they selected, so running the sweeps in more than one process is safe. It is also pointless: one worker keeps up with any number of API replicas. Run a single instance.</p><p>The worker does not provision. The realms, clients, permissions and the admin user are still written by the API process on startup.</p><h2 id="when-to-deploy-one" tabindex="-1">When to deploy one <a class="header-anchor" href="#when-to-deploy-one" aria-label="Permalink to &quot;When to deploy one&quot;">​</a></h2><p>Most deployments do not need a worker. A single <code>authup start</code> process already runs the sweeps, and so does every replica of it.</p><p>Deploy a worker when you run more than one API replica and want the sweeps to happen once rather than N times, or when you want the API replicas to hold no scheduled work at all, so scaling them up and down never affects retention.</p><h2 id="turning-the-sweeps-off-in-the-api" tabindex="-1">Turning the sweeps off in the API <a class="header-anchor" href="#turning-the-sweeps-off-in-the-api" aria-label="Permalink to &quot;Turning the sweeps off in the API&quot;">​</a></h2><p><code>core.worker.enabled</code> reads the same in both modes. Under the default mode it says whether the API runs the worker alongside itself, so a plain <code>start</code> needs nothing set. The API side is where you hand the sweeps over:</p><div class="vp-code-group vp-adaptive-theme"><div class="tabs"><input type="radio" name="group-5PsmJ" id="tab-F6-QeAm" checked><label data-title=".env" for="tab-F6-QeAm">.env</label><input type="radio" name="group-5PsmJ" id="tab-LTw3Cj7"><label data-title="authup.yml" for="tab-LTw3Cj7">authup.yml</label></div><div class="blocks"><div class="language-dotenv vp-adaptive-theme active"><button title="Copy Code" class="copy"></button><span class="lang">dotenv</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">WORKER_ENABLED</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">false</span></span></code></pre></div><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">core</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  worker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span></code></pre></div></div></div><p>Set that on every API replica, and only once a worker is actually running. With it set and no worker, nothing sweeps: expired sessions, tokens and audit events accumulate until a process that runs the sweeps starts.</p><p>Worker mode requires the key. <code>start worker</code> refuses to boot while <code>core.worker.enabled</code> is false, naming the key in the error, rather than coming up idle and sweeping nothing. A worker that reads the same <code>authup.yml</code> as the API replicas therefore has to override it in its own environment:</p><div class="language-dotenv vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">dotenv</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">WORKER_ENABLED</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">true</span></span></code></pre></div><h2 id="schema-ownership" tabindex="-1">Schema ownership <a class="header-anchor" href="#schema-ownership" aria-label="Permalink to &quot;Schema ownership&quot;">​</a></h2><p>One process must own the schema, and the worker is never that process. It verifies at startup that no migration is pending and refuses to boot otherwise, rather than applying DDL of its own. That holds regardless of <code>migrationEnabled</code>, which the worker does not consult. SQLite is the one exception: it ships no migrations at all, so a SQLite worker synchronizes its schema like every other process.</p><p>So a deployment that adds a worker also has to decide who migrates. The recommendation is to let neither long-running process do it:</p><ol><li>Run <code>authup migration run</code> (container command: <code>migration run</code>) as a one-off step, and let it finish.</li><li>Start the API replicas and the worker.</li></ol><p>Set <code>MIGRATION_ENABLED=false</code> on the API replicas to make that ordering explicit. Startup then verifies the schema instead of migrating it, and a replica that comes up against an out-of-date database fails loudly instead of racing its siblings for the DDL. The <code>migration</code> CLI command is unaffected by the flag, and SQLite ignores it entirely (it has no migrations and always synchronizes its schema).</p><p>Keeping the default <code>MIGRATION_ENABLED=true</code> on the API is fine for a single replica. It is the multi-replica rollout where concurrent boots would each try to apply the same migration.</p><h2 id="docker-compose" tabindex="-1">Docker Compose <a class="header-anchor" href="#docker-compose" aria-label="Permalink to &quot;Docker Compose&quot;">​</a></h2><p>The worker is the same image and the same configuration as the API. Give it the database and Redis settings the API has, add the command, and take away the healthcheck: the image probes an HTTP port the worker does not open, so the container would be reported unhealthy forever.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">version</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;3.8&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">services</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    server-core</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">authup/authup:latest</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        container_name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">server-core</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        restart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">unless-stopped</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        ports</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;3000:3000&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        environment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">PUBLIC_URL=http://localhost:3000</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_TYPE=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_HOST=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_PORT=5432</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_USERNAME=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_PASSWORD=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_DATABASE=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">REDIS=redis://redis:6379</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            # the sweeps run in the worker below</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">WORKER_ENABLED=false</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">start</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # one instance is enough, whatever the API scales to</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    server-core-worker</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        image</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">authup/authup:latest</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        container_name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">server-core-worker</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        restart</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">unless-stopped</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        environment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_TYPE=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_HOST=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_PORT=5432</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_USERNAME=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_PASSWORD=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">DB_DATABASE=postgres</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">REDIS=redis://redis:6379</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        # the image healthcheck probes an HTTP port this process never opens</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        healthcheck</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            disable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        command</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">start worker</span></span></code></pre></div><p>The worker needs no published port of its own. It does need the same database as the API, since the sweeps are plain deletes against the shared schema. This split is for the server databases (MySQL and Postgres): a SQLite worker container would open its own database file inside the container and sweep nothing of the API&#39;s data, so a SQLite deployment keeps the sweeps in the API process instead. Neither container carries a volume: the image keeps no durable state, and the worker&#39;s rotating file logs under <code>LOG_DIRECTORY_PATH</code> are ephemeral without one. Mount <code>/var/log/authup</code> to keep them; the console output for <code>docker logs</code> remains either way.</p><h2 id="kubernetes" tabindex="-1">Kubernetes <a class="header-anchor" href="#kubernetes" aria-label="Permalink to &quot;Kubernetes&quot;">​</a></h2><p>The same split, with the migration step as its own object:</p><ul><li>A <code>Job</code> running <code>migration run</code>, as a pre-install and pre-upgrade hook so it completes before the workloads roll.</li><li>The API <code>Deployment</code>, with <code>WORKER_ENABLED=false</code> and <code>MIGRATION_ENABLED=false</code>. Any number of replicas.</li><li>The worker <code>Deployment</code>, <code>replicas: 1</code>, command <code>start worker</code>, with no readiness or liveness HTTP probe. Give it <code>WORKER_ENABLED=true</code> when it shares the API&#39;s <code>ConfigMap</code>, which sets that key to false.</li></ul><p>Leaving the worker without probes is deliberate. There is no port to probe, and a liveness probe over <code>exec</code> would add little: the sweeps are scheduled inside the process, and a process that dies is restarted by the kubelet regardless. What is worth watching is the log, since a sweep that keeps failing (a lost database connection, for example) is caught and retried on the next tick rather than crashing the process.</p>`,35)])])}const E=e(n,[["render",h]]);export{c as __pageData,E as default};
