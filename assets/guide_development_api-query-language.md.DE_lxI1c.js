import{_ as t,o as a,c as s,a0 as i}from"./chunks/framework.BFac62L4.js";const u=JSON.parse('{"title":"Query Language","description":"","frontmatter":{},"headers":[],"relativePath":"guide/development/api-query-language.md","filePath":"guide/development/api-query-language.md"}'),o={name:"guide/development/api-query-language.md"};function n(d,e,r,h,l,c){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="query-language" tabindex="-1">Query Language <a class="header-anchor" href="#query-language" aria-label="Permalink to &quot;Query Language&quot;">​</a></h1><p>Every collection <code>GET</code> accepts the same five query parameters: which rows come back, which columns of them, in which order, with which relations attached, and how many at a time. The vocabulary is per entity and is published rather than guessed. This page defines the grammar; <a href="./api-query-reference.html">Query Reference</a> lists the keys each entity accepts.</p><p>Nothing here needs an SDK. The whole surface is URL query parameters and JSON.</p><h2 id="the-five-parameters" tabindex="-1">The five parameters <a class="header-anchor" href="#the-five-parameters" aria-label="Permalink to &quot;The five parameters&quot;">​</a></h2><table tabindex="0"><thead><tr><th>URL parameter</th><th>Purpose</th><th>Description key</th></tr></thead><tbody><tr><td><code>filter</code></td><td>which rows</td><td><code>filters</code></td></tr><tr><td><code>fields</code></td><td>which columns</td><td><code>fields</code></td></tr><tr><td><code>sort</code></td><td>in which order</td><td><code>sorts</code></td></tr><tr><td><code>include</code></td><td>which relations</td><td><code>relations</code></td></tr><tr><td><code>page[limit]</code>, <code>page[offset]</code></td><td>how many</td><td><code>pagination</code></td></tr></tbody></table><p>The third column matters because the published description names the parameters in the plural, and three of them do not match the URL spelling: <code>filters</code> describes <code>?filter[...]</code>, <code>relations</code> describes <code>?include</code>, and <code>sorts</code> describes <code>?sort</code>. That is a rapiq convention authup inherits, so a client reading a description has to map the two.</p><h2 id="filter" tabindex="-1">filter <a class="header-anchor" href="#filter" aria-label="Permalink to &quot;filter&quot;">​</a></h2><h3 id="bracket-form" tabindex="-1">Bracket form <a class="header-anchor" href="#bracket-form" aria-label="Permalink to &quot;Bracket form&quot;">​</a></h3><p>One bracket per key. The operator is a prefix, a suffix or both on the VALUE, never a separate parameter:</p><table tabindex="0"><thead><tr><th>Value</th><th>Matches</th><th>Example</th></tr></thead><tbody><tr><td><code>v</code></td><td>equal to</td><td><code>filter[name]=admin</code></td></tr><tr><td><code>a,b</code></td><td>any of</td><td><code>filter[name]=admin,user</code></td></tr><tr><td><code>!v</code></td><td>not equal to</td><td><code>filter[name]=!admin</code></td></tr><tr><td><code>!a,b</code></td><td>none of</td><td><code>filter[name]=!admin,user</code></td></tr><tr><td><code>v~</code></td><td>starts with</td><td><code>filter[name]=adm~</code></td></tr><tr><td><code>~v</code></td><td>ends with</td><td><code>filter[name]=~min</code></td></tr><tr><td><code>~v~</code></td><td>contains</td><td><code>filter[name]=~dmi~</code></td></tr><tr><td><code>&lt;v</code> <code>&lt;=v</code></td><td>less than, at most</td><td><code>filter[expiresAt]=&lt;2026-01-01</code></td></tr><tr><td><code>&gt;v</code> <code>&gt;=v</code></td><td>greater than, at least</td><td><code>filter[expiresAt]=&gt;2026-01-01</code></td></tr></tbody></table><p>Values are coerced: <code>true</code> and <code>false</code> become booleans, <code>null</code> becomes null, a numeric string becomes a number, anything else is a trimmed string. So <code>filter[realmId]=null</code> selects the global rows and <code>filter[builtIn]=!true</code> selects the rest. The three wildcard forms keep their value raw, since a substring is a substring.</p><p>Two things about the leading <code>!</code> are worth knowing before it bites:</p><ul><li>it negates the five operators that have an opposite (<code>equal</code>, <code>any of</code>, and the three wildcards) and is <strong>silently discarded on the comparisons</strong>. <code>filter[expiresAt]=!&lt;=2026</code> is read as <code>&lt;=2026</code>, not as its negation. This is a frozen wire quirk, kept because changing it would silently redefine existing clients&#39; queries.</li><li>it is part of the value, so it has to survive URL encoding like any other character.</li></ul><p>A dotted key traverses a relation, and the join is implied: <code>filter[realm.name]=master</code> filters roles by their realm&#39;s name without <code>include=realm</code>. The key is resolved against the RELATED entity&#39;s own allow-list, and the hop is authorized exactly as the include would be, so a caller who may not read realms gets the whole condition dropped rather than an error.</p><h3 id="expression-form" tabindex="-1">Expression form <a class="header-anchor" href="#expression-form" aria-label="Permalink to &quot;Expression form&quot;">​</a></h3><p>The same parameter also accepts a nested expression, which is the only way to express a disjunction:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>filter=and(eq(realmId,null),or(contains(name,&#39;adm&#39;),contains(displayName,&#39;Adm&#39;)))</span></span></code></pre></div><p>Functions: <code>and</code>, <code>or</code>, <code>not</code>, <code>eq</code>, <code>lt</code>, <code>lte</code>, <code>gt</code>, <code>gte</code>, <code>contains</code>, <code>startsWith</code>, <code>endsWith</code>, <code>in</code>, <code>nin</code>, <code>elemMatch</code>, <code>size</code>. Membership is variadic: <code>in(name,&#39;admin&#39;,&#39;user&#39;)</code>.</p><p><strong>Every literal is quoted with single quotes, except <code>null</code>.</strong> <code>eq(builtIn,&#39;true&#39;)</code> is a boolean true, and a bare <code>eq(builtIn,true)</code> is a syntax error, because an unquoted word is a field reference. This is the one part of the dialect that catches everyone once.</p><p>The form is detected from the value, so it needs no opt-in. <code>codec=url-expression</code> alongside it states the dialect explicitly, which is worth doing when a value could plausibly parse either way.</p><p>A top-level <code>or(...)</code> stays a disjunction, and nothing folds its branches into the surrounding conjunction. It does have to satisfy the index rule below on its own, though: an <code>or</code> counts as anchored only when every one of its branches is, and a bare one has no sibling condition to anchor on its behalf. Scoping it, as the example above does, is what lets a branch ride along.</p><h2 id="fields" tabindex="-1">fields <a class="header-anchor" href="#fields" aria-label="Permalink to &quot;fields&quot;">​</a></h2><p><code>fields=id,name</code> projects a subset. <code>fields[$root]=id&amp;fields[realm]=name</code> projects per relation, with <code>$root</code> naming the entity itself.</p><p>Each entity has two halves to its column list: the ones returned when the parameter is absent, and the ones that have to be asked for by name. The second half is small and deliberate (<code>client.secret</code>, <code>user.email</code>, <code>key.certificate</code>), and the reference states it per entity.</p><p>An <code>include</code> narrows the joined relation to its own default projection, so <code>include=realm</code> returns the realm&#39;s ordinary columns rather than all of them.</p><h2 id="sort" tabindex="-1">sort <a class="header-anchor" href="#sort" aria-label="Permalink to &quot;sort&quot;">​</a></h2><p><code>sort=name</code> ascends, <code>sort=-name</code> descends, and several keys are comma separated.</p><p>A multi-key sort is honoured only when the key sequence is a <strong>leftmost prefix of one of the entity&#39;s declared indexes</strong>. It is otherwise dropped whole, and the rows come back unordered rather than as an error. <code>sort=name,-createdAt</code> on roles is exactly that case: both keys sort individually, no index leads with both, so neither applies. The reference prints each entity&#39;s index prefixes for this reason.</p><h2 id="include" tabindex="-1">include <a class="header-anchor" href="#include" aria-label="Permalink to &quot;include&quot;">​</a></h2><p><code>include=realm</code> attaches one relation, comma separated for several, dotted for a nested hop (<code>include=user.realm</code>).</p><p>Every hop is authorized separately. A relation the caller may not read is stripped from the include, and every filter, sort and field key that traversed it goes with it. The request still succeeds, with the un-joined row shape. That is deliberate: a list endpoint that answered 403 because one optional relation was not readable would be unusable for a caller whose permissions vary by row.</p><h2 id="page" tabindex="-1">page <a class="header-anchor" href="#page" aria-label="Permalink to &quot;page&quot;">​</a></h2><p><code>page[limit]</code> and <code>page[offset]</code>. A limit above the entity&#39;s maximum is <strong>clamped, not rejected</strong>, so a client asking for 5000 rows receives the maximum and has to page. The response&#39;s <code>meta.total</code> is the count before pagination, and it is exact: authorization runs as a <code>WHERE</code> clause rather than as a post-filter wherever it can be expressed as one.</p><h2 id="what-fails-and-how" tabindex="-1">What fails, and how <a class="header-anchor" href="#what-fails-and-how" aria-label="Permalink to &quot;What fails, and how&quot;">​</a></h2><p>The parameters fail soft. An unknown key in <code>fields</code>, <code>sort</code>, <code>include</code> or the bracket <code>filter</code> form is dropped and the rest of the request is served, because a client built against a newer release should degrade rather than break.</p><p>The exception is the <strong>expression</strong> filter form, which answers <code>400</code>. It is a single value that either parses or does not, so a dropped key there would silently widen the result set, and a query meant to select one row would return the table. An unknown key inside it is rejected for the same reason.</p><p>Two more rules narrow what is accepted, and both come from the underlying indexes:</p><ul><li>every <code>AND</code> group of a filter needs at least one condition on an index-leading key, and an <code>or(...)</code> counts only when every branch has one.</li><li><code>createdAt</code> and <code>updatedAt</code> are sortable but deliberately not filterable. They are stored through a transformer that applies on read and not to a comparison, so a filter on them would compare an ISO string against the driver&#39;s own storage format and quietly return the wrong rows. The <code>varchar(28)</code> ISO columns that are written as plain strings (<code>session.expiresAt</code>, <code>sessionToken.expiresAt</code>) are filterable and compare correctly.</li></ul><h2 id="discovering-the-vocabulary" tabindex="-1">Discovering the vocabulary <a class="header-anchor" href="#discovering-the-vocabulary" aria-label="Permalink to &quot;Discovering the vocabulary&quot;">​</a></h2><p>Three surfaces publish the same descriptions, so nothing has to be hard-coded against a release:</p><ul><li><strong><code>meta.schema</code></strong> on every query-capable <code>GET</code>. The response that carries the rows also carries what could have been asked of them.</li><li><strong><code>GET /schemas</code></strong> returns every entity&#39;s description in one call, and <strong><code>GET /schemas/:name</code></strong> one of them. Both are authenticated. <code>meta.hash</code> fingerprints the whole surface, so a client that cached the descriptions compares one value instead of diffing them, and <code>meta.recordParameters</code> names the subset a single-record read accepts. An operator may switch these routes off (<code>QUERY_SCHEMA_DISCOVERY_ENABLED=false</code>), in which case both answer <code>404</code>; <code>meta.schema</code> is unaffected.</li><li><strong>the OpenAPI document</strong>, browsable at <code>/docs</code> and machine-readable at <code>/docs/openapi.json</code>. Each query-capable operation declares the five parameters and points at the root <code>x-authup-schemas</code> map for its full description, and <code>x-authup-schema-hash</code> is the same fingerprint.</li></ul><p>A description reads like this:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;role&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;strict&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;indexes&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;clientId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;realmId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;displayName&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;fields&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;allowed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;filters&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;allowed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;caseSensitive&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;indexed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;anchor&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;sorts&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;allowed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;default&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;indexed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;relations&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;allowed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;realm&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;schemas&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;realm&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;realm&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;pagination&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;maxLimit&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">50</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Reading rules:</p><ul><li>the shape is <strong>normalized</strong>: every described parameter carries every constraint key. <code>null</code> means the constraint was never declared, an empty array means an explicit &quot;nothing allowed&quot;.</li><li><code>default</code> and <code>allowed</code> under <code>fields</code> are <strong>disjoint halves of one allow-list</strong>, not a subset and its superset. What may be selected is their union; what comes back without the parameter is <code>default</code>, or everything when <code>default</code> is null.</li><li>relation vocabulary is <strong>referenced, not expanded</strong>. <code>relations.schemas</code> names the schema governing each relation, which is where a dotted key&#39;s own allow-list lives. The two names differ where a relation is not named after its target, as <code>client.accessPolicy</code> is described by <code>policy</code>.</li><li><code>indexes</code> is the index topology the two rules above resolve against.</li><li>the description is the <strong>static upper bound</strong>. It says what may be asked, never what a given caller may read: the relation gate and the per-column visibility conditions narrow the answer per request, silently and by design.</li></ul><h2 id="single-record-reads" tabindex="-1">Single-record reads <a class="header-anchor" href="#single-record-reads" aria-label="Permalink to &quot;Single-record reads&quot;">​</a></h2><p>A single-record <code>GET</code> accepts <code>fields</code> and <code>include</code> only. It processes no filter, no sort and no page, so its <code>meta.schema</code> advertises the two-parameter subset rather than the whole vocabulary.</p><p>Record endpoints are still converging on this: one that does not decode the query yet answers with its default projection instead of failing, and <code>GET /userinfo</code> is a flat OIDC claims document rather than the record envelope. Treat the subset as what a record read will accept, not as what every one of them honours today.</p>`,48)])])}const k=t(o,[["render",n]]);export{u as __pageData,k as default};
