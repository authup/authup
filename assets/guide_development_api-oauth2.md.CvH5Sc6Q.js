import{_ as s,o as t,c as i,a0 as a}from"./chunks/framework.CnTYizyl.js";const k=JSON.parse('{"title":"OAuth2","description":"","frontmatter":{},"headers":[],"relativePath":"guide/development/api-oauth2.md","filePath":"guide/development/api-oauth2.md"}'),n={name:"guide/development/api-oauth2.md"};function o(d,e,h,r,l,c){return t(),i("div",null,[...e[0]||(e[0]=[a(`<h1 id="oauth2" tabindex="-1">OAuth2 <a class="header-anchor" href="#oauth2" aria-label="Permalink to &quot;OAuth2&quot;">​</a></h1><p>Authup implements the OAuth2 (including PKCE) protocol as well as the OpenID specification. The following examples and explanations demonstrate how these flows can be mapped using the Authup API. For the examples, it is assumed that the backend application is running at <code>http://localhost:3000</code>.</p><h2 id="redirect-uris" tabindex="-1">Redirect URIs <a class="header-anchor" href="#redirect-uris" aria-label="Permalink to &quot;Redirect URIs&quot;">​</a></h2><p>When registering a client, you can specify one or more allowed redirect URIs (comma-separated). During authorization, the requested <code>redirect_uri</code> is validated against the registered values.</p><p>Authup supports wildcard patterns in registered redirect URIs as a convenience feature:</p><ul><li><code>*</code> matches any characters within a single path segment (does not cross <code>/</code>)</li><li><code>**</code> matches any characters across path segments</li></ul><p><strong>Examples:</strong></p><table tabindex="0"><thead><tr><th>Registered URI</th><th>Matches</th></tr></thead><tbody><tr><td><code>https://example.com/callback</code></td><td>Exact match only</td></tr><tr><td><code>https://example.com/*</code></td><td><code>https://example.com/callback</code>, <code>https://example.com/auth</code></td></tr><tr><td><code>https://example.com/**</code></td><td><code>https://example.com/callback</code>, <code>https://example.com/auth/done</code></td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Wildcard redirect URIs deviate from <a href="https://datatracker.ietf.org/doc/html/rfc6749#section-3.1.2.3" target="_blank" rel="noreferrer">RFC 6749 §3.1.2.3</a>, which requires exact string matching. This is acceptable when client registration is restricted to administrators. For production deployments, prefer exact redirect URIs to minimize open-redirect risk.</p></div><h2 id="client-secrets" tabindex="-1">Client Secrets <a class="header-anchor" href="#client-secrets" aria-label="Permalink to &quot;Client Secrets&quot;">​</a></h2><p>A confidential client (<code>authMethod: secret</code>) stores its secret in one of three modes. The mode is chosen when the client is created, through the two flags <code>secretHashed</code> and <code>secretEncrypted</code> (never both):</p><table tabindex="0"><thead><tr><th>Mode</th><th>Flags</th><th>Stored as</th><th>Can it be read back?</th></tr></thead><tbody><tr><td><code>plain</code> (default)</td><td>both <code>false</code></td><td>the plaintext</td><td>yes, by a reader whose permissions cover the client</td></tr><tr><td><code>hashed</code></td><td><code>secretHashed: true</code></td><td>a bcrypt hash</td><td>no</td></tr><tr><td><code>encrypted</code></td><td><code>secretEncrypted: true</code></td><td>a cipher blob under the realm&#39;s encryption key</td><td>yes, by a reader whose permissions cover the client</td></tr></tbody></table><p>Whatever the mode, the stored value is projected only to a reader whose permissions cover the client, or to the client itself; the hash of a <code>hashed</code> secret is no exception, since a hash of an admin-chosen value is offline-crackable. A reader outside that reach gets the row without the field.</p><p><code>encrypted</code> is the recoverable mode that keeps the plaintext out of the database. The value is encrypted at rest (AES-256-GCM) under the client realm&#39;s encryption key, the same automatically generated per-realm key that protects MFA seeds. A reader whose permissions cover the client gets the decrypted secret back on any read that projects the field (<code>?fields=+secret</code>; the default projection omits <code>secret</code>), exactly as for a <code>plain</code> secret; a read never returns the ciphertext. The secret is tied to that key&#39;s lifecycle: while the key is disabled such clients cannot authenticate, and deleting the key destroys their secrets, so <code>DELETE /keys/:id</code> answers <code>409</code> while client secrets (or identity-provider secrets, which the same key protects) reference it and needs <code>force</code> to proceed. Set <code>secretsEncryptionKey</code> (<code>SECRETS_ENCRYPTION_KEY</code>, see the <a href="./../deployment/configuration-server-core.html">server configuration</a>) so the realm key itself is wrapped at rest. Declaring <code>secretHashed</code> and <code>secretEncrypted</code> together answers <code>400</code>.</p><p>The flags are create-time properties. An update (<code>POST /clients/:id</code>) ignores them; the mode changes only through the rotation endpoint, together with a new secret. A client created in <code>hashed</code> or <code>encrypted</code> mode answers with the stored form in <code>secret</code> (the hash, or the cipher blob), not with the value you sent, so keep the plaintext you chose; an encrypted secret can be read back later with <code>GET /clients/:id?fields=+secret</code>.</p><h3 id="rotating-a-secret" tabindex="-1">Rotating a secret <a class="header-anchor" href="#rotating-a-secret" aria-label="Permalink to &quot;Rotating a secret&quot;">​</a></h3><p><code>POST /clients/:id/secret</code> replaces the secret and, optionally, the mode. Both body fields are optional: without <code>secret</code> the server generates one, without <code>mode</code> the client keeps its current mode. <code>mode</code> is one of <code>plain</code>, <code>hashed</code> or <code>encrypted</code>.</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/clients/YOUR_CLIENT_ID/secret&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Authorization: Bearer YOUR_ACCESS_TOKEN&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/json&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{ &quot;mode&quot;: &quot;hashed&quot; }&#39;</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;YOUR_CLIENT_ID&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;acme-app&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;authMethod&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;secret&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;secretHashed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;secretEncrypted&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;meta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;secret&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GENERATED_PLAINTEXT&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>meta.secret</code> is the plaintext, returned in this response only; a hashed secret cannot be read back later. The previous secret stops working as soon as the response is sent, so update the application before it next authenticates. The same route exists under a realm, <code>POST /realms/:realmId/clients/:id/secret</code>.</p><p>Rotating into <code>encrypted</code> mode works the same way, and the secret stays readable afterwards:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/clients/YOUR_CLIENT_ID/secret&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Authorization: Bearer YOUR_ACCESS_TOKEN&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/json&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{ &quot;mode&quot;: &quot;encrypted&quot; }&#39;</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;data&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;YOUR_CLIENT_ID&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;acme-app&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;authMethod&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;secret&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;secretHashed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;secretEncrypted&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;meta&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;secret&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;GENERATED_PLAINTEXT&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>A client whose <code>secretEncrypted</code> flag predates encrypted storage still holds a plaintext and keeps authenticating with it. Rotating it without a <code>mode</code> stores the new secret encrypted, which is what the flag was meant to say.</p><p>The caller needs the <code>client_update</code> permission for the client&#39;s realm. A client may rotate its own secret with <code>POST /clients/@me/secret</code> under <code>client_self_manage</code>, keeping its current mode; a mode change is an administrator&#39;s call and answers <code>403</code> for a self-managing client. A public client (<code>authMethod: none</code>) has no secret and answers <code>400</code>.</p><p>Every rotation is recorded as a <code>clientSecretRotated</code> event in the security event log. The event carries the mode and never the secret.</p><h3 id="changing-the-secret-of-a-plain-client" tabindex="-1">Changing the secret of a plain client <a class="header-anchor" href="#changing-the-secret-of-a-plain-client" aria-label="Permalink to &quot;Changing the secret of a plain client&quot;">​</a></h3><p>While a client is in <code>plain</code> mode, <code>POST /clients/:id</code> with a <code>secret</code> still sets a new plaintext. On a hashed or encrypted client the same request answers <code>400</code> and names the rotation endpoint, so an update can never downgrade a protected secret.</p><h2 id="flows" tabindex="-1">Flows <a class="header-anchor" href="#flows" aria-label="Permalink to &quot;Flows&quot;">​</a></h2><h3 id="_1-password-flow" tabindex="-1">1. Password Flow <a class="header-anchor" href="#_1-password-flow" aria-label="Permalink to &quot;1. Password Flow&quot;">​</a></h3><p>The Password Grant Flow is used when the client application can directly access the user&#39;s credentials. This flow allows the client to exchange the user&#39;s username and password for an access token. It is most often used for trusted applications like mobile apps or desktop apps.</p><h4 id="request" tabindex="-1">Request <a class="header-anchor" href="#request" aria-label="Permalink to &quot;Request&quot;">​</a></h4><p>To obtain an access token using the Password Grant Flow, send a POST request with the user&#39;s credentials:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/token&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/x-www-form-urlencoded&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;grant_type=password&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;username=USER_USERNAME&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;password=USER_PASSWORD&#39;</span></span></code></pre></div><p>The user is resolved within a single realm. Pass <code>realm_id</code> or <code>realm_name</code> (both accept a realm UUID or name) to select it; when neither is provided — or the provided value does not match any realm — the <strong>master</strong> realm is used. In multi-realm deployments, users outside the master realm must therefore include a realm parameter:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/token&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/x-www-form-urlencoded&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;grant_type=password&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;username=USER_USERNAME&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;password=USER_PASSWORD&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;realm_id=REALM_ID_OR_NAME&#39;</span></span></code></pre></div><p>A confidential client authenticating on this grant by <strong>name</strong> must belong to the same realm as the user; clients identified by UUID are unaffected. The realm parameter constrains <strong>name</strong> resolution only — a UUID-identified user is resolved globally by primary key, and the issued token always carries the user&#39;s actual realm.</p><p>The same <code>realm_id</code> / <code>realm_name</code> semantic (master fallback included) applies on the <code>authorization_code</code> and <code>refresh_token</code> grants, where it scopes client-by-name resolution: a client identified by <strong>name</strong> on those grants resolves within the hinted realm (master when no hint is given). Pass the realm parameter — or the client&#39;s UUID — when the client lives outside the master realm.</p><h4 id="response" tabindex="-1">Response <a class="header-anchor" href="#response" aria-label="Permalink to &quot;Response&quot;">​</a></h4><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;access_token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ACCESS_TOKEN&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;token_type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bearer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;expires_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3600</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_2-client-credentials-flow" tabindex="-1">2. Client Credentials Flow <a class="header-anchor" href="#_2-client-credentials-flow" aria-label="Permalink to &quot;2. Client Credentials Flow&quot;">​</a></h3><p>The Client Credentials Flow is typically used for machine-to-machine communication, where the application needs to authenticate without the need for user involvement.</p><h4 id="request-1" tabindex="-1">Request <a class="header-anchor" href="#request-1" aria-label="Permalink to &quot;Request&quot;">​</a></h4><p>To obtain an access token using the Client Credentials Flow, send a POST request to the token endpoint:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/token&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/x-www-form-urlencoded&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;grant_type=client_credentials&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;client_id=YOUR_CLIENT_ID&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;client_secret=YOUR_CLIENT_SECRET&#39;</span></span></code></pre></div><h4 id="response-1" tabindex="-1">Response <a class="header-anchor" href="#response-1" aria-label="Permalink to &quot;Response&quot;">​</a></h4><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;access_token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ACCESS_TOKEN&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;token_type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bearer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;expires_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3600</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_3-refresh-token" tabindex="-1">3. Refresh Token <a class="header-anchor" href="#_3-refresh-token" aria-label="Permalink to &quot;3. Refresh Token&quot;">​</a></h3><p>If your access token expires, you can use the Refresh Token Flow to obtain a new access token using the refresh token.</p><h4 id="request-2" tabindex="-1">Request <a class="header-anchor" href="#request-2" aria-label="Permalink to &quot;Request&quot;">​</a></h4><p>To request a new access token, use the following POST request:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/token&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/x-www-form-urlencoded&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;grant_type=refresh_token&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;refresh_token=YOUR_REFRESH_TOKEN&#39;</span></span></code></pre></div><h4 id="response-2" tabindex="-1">Response <a class="header-anchor" href="#response-2" aria-label="Permalink to &quot;Response&quot;">​</a></h4><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;access_token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;***&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;refresh_token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;xxx&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;token_type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;bearer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;expires_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3600</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h3 id="_4-authorization-code-flow-openid-connect" tabindex="-1">4. Authorization Code Flow (OpenID Connect) <a class="header-anchor" href="#_4-authorization-code-flow-openid-connect" aria-label="Permalink to &quot;4. Authorization Code Flow (OpenID Connect)&quot;">​</a></h3><p>Browser-based applications redirect the user to Authup&#39;s hosted <code>/authorize</code> page, which handles login and consent. On success the browser is redirected back to the <code>redirect_uri</code> with a <code>code</code>, which is then exchanged at <code>/token</code> (<code>grant_type=authorization_code</code>). Public clients must use PKCE (<code>code_challenge</code> / <code>code_verifier</code>) and <code>state</code>.</p><p><code>response_type=code</code> is the <strong>only</strong> supported response type (OAuth 2.1 posture): the implicit and hybrid response types (<code>token</code>, <code>id_token</code>, <code>none</code>) are rejected with <code>unsupported_response_type</code> — tokens are never delivered via the redirect URL. RPs still relying on implicit must migrate to the code flow with PKCE.</p><h4 id="authorize-request" tabindex="-1">Authorize request <a class="header-anchor" href="#authorize-request" aria-label="Permalink to &quot;Authorize request&quot;">​</a></h4><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET http://localhost:3000/authorize</span></span>
<span class="line"><span>  ?response_type=code</span></span>
<span class="line"><span>  &amp;client_id=YOUR_CLIENT_ID</span></span>
<span class="line"><span>  &amp;realm_id=YOUR_REALM_ID</span></span>
<span class="line"><span>  &amp;redirect_uri=https://app.example.com/callback</span></span>
<span class="line"><span>  &amp;scope=global openid</span></span>
<span class="line"><span>  &amp;state=RANDOM</span></span>
<span class="line"><span>  &amp;code_challenge=CHALLENGE</span></span>
<span class="line"><span>  &amp;code_challenge_method=S256</span></span></code></pre></div><p>The following <a href="https://openid.net/specs/openid-connect-core-1_0.html#AuthRequest" target="_blank" rel="noreferrer">OpenID Connect Core §3.1.2.1</a> parameters are supported:</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>prompt</code></td><td>Space-delimited list of <code>none</code>, <code>login</code>, <code>consent</code>, <code>select_account</code>. <code>none</code> performs silent authentication (no UI): a <code>built_in</code> client with a valid, realm-matching session is auto-consented and redirected with a <code>code</code>; otherwise the OIDC error (<code>login_required</code>, <code>consent_required</code>, or <code>interaction_required</code>) is redirected to the <code>redirect_uri</code>. <strong><code>prompt=none</code> must be driven as a top-level navigation, not a hidden iframe</strong> — the authorize page sends <code>X-Frame-Options: DENY</code> / <code>frame-ancestors &#39;none&#39;</code>, so the classic iframe silent-renew pattern is blocked. <code>select_account</code> shows a &quot;continue as / use another account&quot; chooser when a session already exists; <code>login</code> forces re-authentication (with a banner); <code>consent</code> forces the consent screen. Unknown values are ignored; <code>none</code> combined with any other value is an <code>invalid_request</code>.</td></tr><tr><td><code>max_age</code></td><td>Maximum acceptable age (seconds) of the authentication. If the session is older, the user is asked to re-authenticate (<code>max_age=0</code> forces it).</td></tr><tr><td><code>login_hint</code></td><td>Pre-fills the identifier on the login form.</td></tr><tr><td><code>ui_locales</code></td><td>Space-delimited BCP47 language tags, most preferred first. The hosted pages open in the first well-formed tag, so a visitor arriving from an application rendered in French is not dropped into their browser&#39;s language. A tag Authup has no catalog for renders the fallback catalog rather than failing; the languages worth sending are advertised as <code>ui_locales_supported</code>.</td></tr></tbody></table><p>The freshness window for <code>prompt=login</code> is configurable via <code>promptLoginMaxAge</code> (see the <a href="./../deployment/configuration-server-core.html">server configuration</a>).</p><p>One parameter is Authup&#39;s own, next to <code>ui_locales</code> and read the same way:</p><table tabindex="0"><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>ui_color_mode</code></td><td><code>light</code>, <code>dark</code> or <code>system</code>. Opens the hosted pages in the color mode your application renders in. One value rather than a list, since there is nothing to negotiate. OIDC defines nothing for this, so the accepted values are advertised as <code>ui_color_modes_supported</code>.</td></tr></tbody></table><p>Both are <strong>hints, not settings</strong>. They seed the page only while the visitor has made no choice on the Authup origin itself: once they use the language or color-mode switcher there, that choice wins and the hint is ignored. Neither is ever stored. A color mode outside the three is ignored rather than refused, the same forward-compatibility rule <code>prompt</code> follows; a malformed <code>ui_locales</code> list is an <code>invalid_request</code>, like a malformed <code>max_age</code>.</p><p>They ride the authorization request, so they survive a round-trip through an external identity provider.</p><p><strong>An application built on <code>@authup/client-web-kit</code> sends both already.</strong><code>buildAuthorizeURL</code> reads the <code>vc-locale</code> and <code>vc-color-mode</code> cookies its own locale manager and color mode write, so there is nothing to wire: what the visitor picked in your application is what the hosted pages open in. It sends nothing while they have picked nothing, since <code>auto</code> and <code>system</code> resolve to the same browser on both sides. Pass <code>uiLocales</code> / <code>uiColorMode</code> to override either, or <code>&#39;&#39;</code> to opt out.</p><div class="tip custom-block"><p class="custom-block-title">Hints reach the anonymous pages; the claims below do not</p><p>The pages these hints are for (<code>/authorize</code>, register, password recovery, device verification) are anonymous, so there is no token to read a preference from. The <code>locale</code> and <code>color_mode</code> claims cover everything after sign-in; the hints cover the first page. Send both.</p></div><p>The resulting <code>id_token</code> includes the OIDC <code>auth_time</code> (the real authentication time) and <code>sid</code> (session id) claims.</p><h4 id="the-locale-and-color-mode-claims" tabindex="-1">The <code>locale</code> and <code>color_mode</code> claims <a class="header-anchor" href="#the-locale-and-color-mode-claims" aria-label="Permalink to &quot;The \`locale\` and \`color_mode\` claims&quot;">​</a></h4><p>A user&#39;s language and color mode are account-level preferences, stored as two user attributes named <code>locale</code> and <code>colorMode</code>, and served as claims: <code>locale</code> is the OIDC standard claim (Core 5.1), <code>color_mode</code> is Authup&#39;s own, <code>light</code>, <code>dark</code> or <code>system</code>. Both appear on the <code>id_token</code> and on both introspection routes. A user without the attribute gets no claim, not a <code>null</code>. The access token carries neither, and <code>/userinfo</code> serves the user record with its attributes flattened under their own names, so it answers <code>locale</code> and <code>colorMode</code> rather than the claim names.</p><p>Like every claim, the value in a token is the one at issuance. Introspection rebuilds the claims from the row on every call, so <strong><code>POST /token/introspect</code> and <code>GET /sessions/@me/introspect</code> answer the current preference</strong>, which is what an application should read when it wants to follow a change the user made elsewhere.</p><p>The two rows are ordinary user attributes: a user writes their own under <code>user_self_manage</code>, an administrator under <code>user_update</code>. What makes them reserved is that their value is checked (a BCP47 tag of at most 35 characters; one of the three color modes) and that Authup&#39;s own consoles and <code>@authup/client-web-kit</code> seed the browser from them and write a switcher change back, so a preference made in one application is the one every application opens in.</p><h4 id="discovery" tabindex="-1">Discovery <a class="header-anchor" href="#discovery" aria-label="Permalink to &quot;Discovery&quot;">​</a></h4><p>Each realm exposes an OpenID Provider metadata document at <code>GET /realms/&lt;realm&gt;/.well-known/openid-configuration</code>, advertising the <code>authorization_endpoint</code>, <code>token_endpoint</code>, <code>revocation_endpoint</code> (<code>/token/revoke</code>), <code>end_session_endpoint</code> (<code>/logout</code>), <code>device_authorization_endpoint</code> (<code>/device_authorization</code>, see <a href="#_8-device-authorization-grant-rfc-8628">Device Authorization Grant</a>), <code>jwks_uri</code>, <code>prompt_values_supported</code>, <code>ui_locales_supported</code>, <code>ui_color_modes_supported</code>, <code>grant_types_supported</code>, and the two back-channel logout flags <code>backchannel_logout_supported</code> and <code>backchannel_logout_session_supported</code> (both <code>true</code>, see <a href="#_7-back-channel-logout">Back-Channel Logout</a>).</p><p><code>grant_types_supported</code> lists the five grants Authup implements: <code>authorization_code</code>, <code>client_credentials</code>, <code>password</code>, <code>refresh_token</code> and <code>urn:ietf:params:oauth:grant-type:device_code</code>. It describes the server, not a client: each client&#39;s own <code>grantTypes</code> allowlist decides what that client may use. With <code>mtlsPublicUrl</code> set, <code>mtls_endpoint_aliases</code> carries <code>device_authorization_endpoint</code> next to the token endpoint alias, because a <code>tls</code> client authenticates at the device endpoint exactly as it does at <code>/token</code>.</p><p>The management read of a realm, <code>GET /realms/:id</code>, carries the three values an integrator copies out of that document under <code>meta.endpoints</code>: <code>issuer</code>, <code>openidConfiguration</code> and <code>jwks</code>. They are derived from the deployment&#39;s public url and the realm name, so a renamed realm answers with a new issuer. The realm collection, <code>GET /realms</code>, does not carry them: derive them from <code>&lt;publicUrl&gt;/realms/&lt;name&gt;</code> (the issuer; the discovery document and the JWKS sit under it) or read the record. The admin console shows them on the realm&#39;s page.</p><h3 id="_5-federated-login-external-identity-providers" tabindex="-1">5. Federated login (external identity providers) <a class="header-anchor" href="#_5-federated-login-external-identity-providers" aria-label="Permalink to &quot;5. Federated login (external identity providers)&quot;">​</a></h3><p>Nothing changes for a relying party when the person signs in through an external identity provider. The application sends the same authorization request, the person picks a provider on the hosted <code>/authorize</code> page instead of typing a password, and the <code>code</code> arrives at the application&#39;s <code>redirect_uri</code> exactly as it does for any other login. The provider round-trip happens between Authup and that provider; the application never sees it.</p><p>Two things are worth knowing when reading the resulting tokens:</p><ul><li><code>amr</code> is <code>[&quot;ext&quot;]</code>: the subject authenticated at an external provider. It carries <code>&quot;otp&quot;</code> in addition when a second factor was verified.</li><li><code>acr</code> is absent for a federated login that verified no local factor. Authup checked no credential of its own, so it asserts no authentication context class. A password login reports <code>urn:authup:pwd</code>, and any login that verified a factor reports <code>urn:authup:mfa</code>.</li></ul><p>Authup does not require its own second factor on top of a federated login, because the provider is the authentication authority (see <a href="./../user/identity-providers.html">Identity Providers</a>). An application that needs a proof regardless can request one:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">GET</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /authorize?...&amp;acr_values=urn:authup:mfa</span></span></code></pre></div><p>Per <a href="https://openid.net/specs/openid-connect-core-1_0.html#acrSemantics" target="_blank" rel="noreferrer">OpenID Connect Core §5.5.1.1</a> this is voluntary, so read the <code>acr</code> you get back rather than assuming the request was satisfiable: a person who holds no factor at all cannot produce one, and the token then reports what was actually achieved.</p><h3 id="_6-rp-initiated-logout" tabindex="-1">6. RP-Initiated Logout <a class="header-anchor" href="#_6-rp-initiated-logout" aria-label="Permalink to &quot;6. RP-Initiated Logout&quot;">​</a></h3><p>To end the Authup session when the user logs out of your application, redirect the browser to the <code>end_session_endpoint</code> (<a href="https://openid.net/specs/openid-connect-rpinitiated-1_0.html" target="_blank" rel="noreferrer">OpenID Connect RP-Initiated Logout 1.0</a>):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>GET http://localhost:3000/logout</span></span>
<span class="line"><span>  ?id_token_hint=THE_ID_TOKEN</span></span>
<span class="line"><span>  &amp;client_id=YOUR_CLIENT_ID</span></span>
<span class="line"><span>  &amp;post_logout_redirect_uri=https://app.example.com/after-logout</span></span>
<span class="line"><span>  &amp;state=RANDOM</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Parameter</th><th>Description</th></tr></thead><tbody><tr><td><code>id_token_hint</code></td><td>An <code>id_token</code> previously issued to the user. When its signature verifies (an expired token is accepted — bounded by <code>endSessionHintGracePeriod</code> / <code>END_SESSION_HINT_GRACE_PERIOD</code> when configured, unbounded by default) and its subject matches the referenced session, that session is revoked immediately without a confirmation prompt; a subject mismatch is ignored as a no-op.</td></tr><tr><td><code>client_id</code></td><td>The client requesting logout. Cross-checked against the hint&#39;s <code>aud</code> when both are present — note <code>aud</code> carries the client&#39;s <strong>id</strong>, so a name-identified <code>client_id</code> will not match a hint&#39;s <code>aud</code>.</td></tr><tr><td><code>post_logout_redirect_uri</code></td><td>Where to redirect after logout. Honored only when it is an absolute <code>http(s)</code> URL matching one of the client&#39;s registered <code>post_logout_redirect_uri</code> patterns (a dedicated allow-list, separate from the login <code>redirect_uri</code>; same comma-separated wildcard syntax; open-redirect guard); otherwise ignored.</td></tr><tr><td><code>state</code></td><td>Opaque value echoed back on the redirect (only alongside a validated <code>post_logout_redirect_uri</code>).</td></tr></tbody></table><p>Without a valid <code>id_token_hint</code>, <code>/logout</code> serves a page asking the user to confirm sign-out; no state is changed until they confirm. The same applies when the request is malformed (oversized parameters, an invalid <code>post_logout_redirect_uri</code>): every parameter is discarded and the neutral confirm page is served. The <code>realm_id</code> / <code>realm_name</code> hint (scoping a name-identified <code>client_id</code>) is case-insensitive.</p><h3 id="_7-back-channel-logout" tabindex="-1">7. Back-Channel Logout <a class="header-anchor" href="#_7-back-channel-logout" aria-label="Permalink to &quot;7. Back-Channel Logout&quot;">​</a></h3><p>RP-initiated logout lets your application end the Authup session. Back-channel logout (<a href="https://openid.net/specs/openid-connect-backchannel-1_0.html" target="_blank" rel="noreferrer">OpenID Connect Back-Channel Logout 1.0</a>) is the other direction: Authup tells your application when a session it received tokens for was ended elsewhere, by the user on the account console, by an administrator, by another application&#39;s logout, or by refresh-token replay detection.</p><h4 id="registering-the-endpoint" tabindex="-1">Registering the endpoint <a class="header-anchor" href="#registering-the-endpoint" aria-label="Permalink to &quot;Registering the endpoint&quot;">​</a></h4><p>Set the client&#39;s <code>backchannelLogoutUri</code> (admin console client form, or <code>POST /clients/:id</code> with <code>{&quot;backchannelLogoutUri&quot;: &quot;https://app.example.com/logout/backchannel&quot;}</code>). It is one absolute <code>http(s)</code> URL of at most 2000 characters, not a pattern list: no wildcards, no comma-separated values. <code>null</code> (the default, and what the provisioned system clients keep) disables the push for that client.</p><h4 id="the-request" tabindex="-1">The request <a class="header-anchor" href="#the-request" aria-label="Permalink to &quot;The request&quot;">​</a></h4><p>Authup sends one request per affected client:</p><div class="language-http vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">http</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">POST</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /logout/backchannel </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">HTTP</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.1</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Host</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app.example.com</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">Content-Type</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> application/x-www-form-urlencoded</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">logout_token=eyJhbGciOi...</span></span></code></pre></div><p><code>logout_token</code> is a JWT signed with the active signing key of your client&#39;s realm, the realm named in <code>iss</code>, so it is verifiable against that realm&#39;s <code>jwks_uri</code>. Verify it the way the specification asks (section 2.6):</p><table tabindex="0"><thead><tr><th>Claim</th><th>Value</th></tr></thead><tbody><tr><td><code>iss</code></td><td>The realm issuer, as in every other token of that realm</td></tr><tr><td><code>aud</code></td><td>Your client&#39;s id</td></tr><tr><td><code>sub</code></td><td>The subject the session belonged to</td></tr><tr><td><code>sid</code></td><td>The session id, matching the <code>sid</code> of the id_tokens that session issued</td></tr><tr><td><code>events</code></td><td><code>{&quot;http://schemas.openid.net/event/backchannel-logout&quot;: {}}</code></td></tr><tr><td><code>iat</code>, <code>exp</code>, <code>jti</code></td><td>Issued at, expiry (2 minutes after issue), a unique id to reject replays</td></tr><tr><td><code>kind</code></td><td><code>logout_token</code>. No Authup endpoint accepts a token of this kind as a bearer</td></tr></tbody></table><p>A logout token never carries a <code>nonce</code>; refuse one that does, since it could be an id_token in disguise. The <code>typ: logout+jwt</code> header is not set yet, so do not require it. Answer with any <code>2xx</code> once the local session for <code>sid</code> is ended. Applications that keep no per-session state can key on <code>sub</code> and end every session of that user.</p><h4 id="when-it-fires" tabindex="-1">When it fires <a class="header-anchor" href="#when-it-fires" aria-label="Permalink to &quot;When it fires&quot;">​</a></h4><ul><li><code>DELETE /sessions/:id</code> and <code>DELETE /sessions</code> (the account console&#39;s &quot;log out other devices&quot;, an administrator&#39;s force-logout, the sessions UI);</li><li><code>/logout</code> with a verified <code>id_token_hint</code>, once the hosted sign-out page has confirmed it: the <code>GET</code> and <code>form_post</code> bindings are browser navigations that hand over to that page, and the revoke runs on the JSON call the page makes;</li><li>the refresh-token replay reaction, which revokes the whole session.</li></ul><p>A session that simply expires sends nothing: its tokens expire with it. Revoking a single token (<code>POST /token/revoke</code>) sends nothing either, because the session stays alive.</p><h4 id="best-effort" tabindex="-1">Best effort <a class="header-anchor" href="#best-effort" aria-label="Permalink to &quot;Best effort&quot;">​</a></h4><p>Delivery is awaited but never blocks the logout: every affected client is notified concurrently, each request times out after 5 seconds, and a client that is unreachable or answers a non-<code>2xx</code> status is logged on the server and otherwise ignored. The API call that ended the session succeeds regardless. Do not treat a missing push as proof that the session is still alive.</p><p>Every delivery is recorded in the security event log (<code>GET /events?filter[name]=backchannelLogout</code> for the pushes that landed, <code>filter[name]=backchannelLogoutFailed</code> for the ones that did not). A row names the client and the ended session, and its <code>data.jti</code> is the <code>jti</code> of the logout token, so it can be matched against your application&#39;s own log of the tokens it received. A failed row carries the <code>status</code> your endpoint answered with, or the <code>errorCode</code> when no answer arrived (<code>ECONNREFUSED</code>, <code>ENOTFOUND</code>, <code>TimeoutError</code> after the 5 seconds); one carrying no <code>jti</code> at all is a token that could never be signed, so nothing was sent to you. The rows are attributed to the user who was signed out, not to your client, so your own client credentials list none of them: reading another subject&#39;s rows takes the <code>EVENT_READ</code> permission, an administrator&#39;s. Clients without a <code>backchannelLogoutUri</code> are never contacted and leave no row.</p><h3 id="_8-device-authorization-grant-rfc-8628" tabindex="-1">8. Device Authorization Grant (RFC 8628) <a class="header-anchor" href="#_8-device-authorization-grant-rfc-8628" aria-label="Permalink to &quot;8. Device Authorization Grant (RFC 8628)&quot;">​</a></h3><p>The device grant (<a href="https://datatracker.ietf.org/doc/html/rfc8628" target="_blank" rel="noreferrer">RFC 8628</a>) signs a person in on a device that has no browser or no keyboard: a TV, a CLI, a printer. The device asks Authup for a pair of codes and shows the short one. The person opens <code>&lt;publicUrl&gt;/device</code> on a phone or a laptop, enters the code, signs in and approves. Meanwhile the device polls <code>/token</code> until the approval lands.</p><h4 id="enabling-the-grant" tabindex="-1">Enabling the grant <a class="header-anchor" href="#enabling-the-grant" aria-label="Permalink to &quot;Enabling the grant&quot;">​</a></h4><p>The grant is opt-in per client. List <code>urn:ietf:params:oauth:grant-type:device_code</code> in the client&#39;s <code>grantTypes</code> allowlist (the admin console&#39;s client form, <code>POST /clients/:id</code>, or a <a href="./../deployment/provisioning.html#client">provisioning file</a>). An empty <code>grantTypes</code> allows every other grant but not this one, and a client that does not list the URN is refused with <code>unauthorized_client</code> at both endpoints. The exception exists because the flow has no redirect URI to bind its result to: whoever gets a person to type a code approves that device (RFC 8628 §5.4), so no client carries that surface unless an administrator asked for it. A deployment that lists the URN on no client has the grant off.</p><p>The <code>authup</code> CLI is one such device: <code>authup login --client &lt;id&gt;</code> runs this flow against a client the operator lists the URN on (see <a href="./../deployment/cli.html">Sign-in and entity commands</a>).</p><h4 id="request-3" tabindex="-1">Request <a class="header-anchor" href="#request-3" aria-label="Permalink to &quot;Request&quot;">​</a></h4><p><code>POST /device_authorization</code> is form-encoded and takes the client credentials the token endpoint takes. A public client (<code>authMethod: none</code>) identifies itself with <code>client_id</code> alone; a secret sent for a public client is refused.</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/device_authorization&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/x-www-form-urlencoded&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;client_id=YOUR_CLIENT_ID&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;scope=global openid&#39;</span></span></code></pre></div><p>A confidential client (<code>authMethod: secret</code>) authenticates with <code>client_secret</code> in the body or as a Basic <code>Authorization</code> header, never both at once. It authenticates at both endpoints: here, and again on every poll at <code>/token</code>.</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/device_authorization&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/x-www-form-urlencoded&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;client_id=YOUR_CLIENT_ID&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;client_secret=YOUR_CLIENT_SECRET&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;scope=global openid&#39;</span></span></code></pre></div><p><code>scope</code> is optional. Without it the code carries every scope bound to the client. A requested scope must be covered by the client&#39;s bound scopes or carry <code>global</code>; anything beyond that answers <code>insufficient_scope</code> rather than being trimmed, the same rule <code>/authorize</code> applies.</p><p>A client identified by name takes the same <code>realm_id</code> / <code>realm_name</code> hint as on <code>/token</code>: the name resolves within the hinted realm, and within the master realm when no hint is given, so pass the hint or the client&#39;s UUID for a client outside the master realm. A UUID resolves globally. The request accepts no <code>prompt</code>, <code>max_age</code> or <code>acr_values</code>; RFC 8628 defines none.</p><h4 id="response-3" tabindex="-1">Response <a class="header-anchor" href="#response-3" aria-label="Permalink to &quot;Response&quot;">​</a></h4><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;device_code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;DEVICE_CODE&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;user_code&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;BCDF-GHJK&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;verification_uri&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http://localhost:3000/device&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;verification_uri_complete&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;http://localhost:3000/device?user_code=BCDF-GHJK&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;expires_in&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">600</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;interval&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">5</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>device_code</code> is a 64-character secret the device keeps to itself; it never reaches a browser. <code>user_code</code> is eight consonants shown to the person; case and dashes do not matter when it is typed. Both expire after <code>expires_in</code> seconds (10 minutes). <code>verification_uri_complete</code> pre-fills the code, for a QR code or a clickable link; the page still displays the code for confirmation. Failures: <code>invalid_request</code> (mixed credentials, a secret without a <code>client_id</code>, an over-long <code>scope</code>), <code>invalid_client</code> (<code>401</code>: an unknown or inactive client, a wrong or missing secret, a secret on a public client), <code>unauthorized_client</code> (the grant is not listed) and <code>insufficient_scope</code>.</p><h4 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h4><p>The person opens the verification URI, enters the code, signs in (then completes the second factor when one is enrolled) and sees the client&#39;s name, its realm and the scopes it asked for. Signing in is a username and password, or an external identity provider: once a realm is picked the login form offers that realm&#39;s OAuth2 and OpenID Connect providers. A person coming back from a provider is signed in and lands on the code step again, with the code filled in, and confirms it before anything is looked up. The link that starts such a login is an ordinary URL anyone can build for a code of their own, so the page never skips showing the code it is about to act on. Approving or denying is always an explicit click, for a <code>builtIn</code> client as well: the page cannot know which device is asking, so nothing is auto-consented. The approval runs the gates <code>/authorize</code> runs. The person&#39;s realm must match the client&#39;s, a user holding a confirmed authenticator completes the second factor first, <code>mfaRequired</code> routes a user without one through enrollment, and the client&#39;s access policy is evaluated. A wrong or expired code answers one neutral error whatever the reason. After ten misses within ten minutes the page refuses further attempts by that user with <code>429</code> (<code>device_verification_throttled</code>, <code>retryAfter</code> in seconds). Approving or denying a code forgives the misses counted so far, at most once per ten minutes, so a few typos before a successful approval do not add up to a lockout later.</p><p><code>GET /device</code> is one of the hosted auth pages: like <code>/authorize</code> it hands over to the auth console, carrying the <code>user_code</code> it was called with.</p><h4 id="polling" tabindex="-1">Polling <a class="header-anchor" href="#polling" aria-label="Permalink to &quot;Polling&quot;">​</a></h4><p>The device polls <code>/token</code> every <code>interval</code> seconds:</p><div class="language-shell vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">shell</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;http://localhost:3000/token&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;Content-Type: application/x-www-form-urlencoded&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;grant_type=urn:ietf:params:oauth:grant-type:device_code&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;device_code=DEVICE_CODE&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;client_id=YOUR_CLIENT_ID&#39;</span></span></code></pre></div><p>A confidential client adds its <code>client_secret</code> or the Basic header. Success is the ordinary token response, with an <code>id_token</code> when <code>openid</code> was granted. Until then every poll answers an error body carrying Authup&#39;s <code>code</code> and the RFC 8628 <code>error</code>, both top-level fields:</p><table tabindex="0"><thead><tr><th>Condition (evaluation order)</th><th><code>code</code></th><th><code>error</code></th><th>status</th></tr></thead><tbody><tr><td>client auth failed</td><td><code>invalid_client</code></td><td><code>invalid_client</code></td><td>401</td></tr><tr><td>not opted in</td><td><code>unauthorized_client</code></td><td><code>unauthorized_client</code></td><td>400</td></tr><tr><td>unknown / redeemed / flushed / client or realm mismatch</td><td><code>invalid_grant</code></td><td><code>invalid_grant</code></td><td>400</td></tr><tr><td>past <code>expires_at</code> (blob still within retention)</td><td><code>device_code_expired</code></td><td><code>expired_token</code></td><td>400</td></tr><tr><td>polled inside the window</td><td><code>slow_down</code></td><td><code>slow_down</code></td><td>400</td></tr><tr><td>no decision yet</td><td><code>authorization_pending</code></td><td><code>authorization_pending</code></td><td>400</td></tr><tr><td>denied (popped, so once)</td><td><code>access_denied</code></td><td><code>access_denied</code></td><td>400</td></tr><tr><td>approved but the pop lost a race, or the access-policy backstop denies</td><td><code>invalid_grant</code></td><td><code>invalid_grant</code></td><td>400</td></tr></tbody></table><p>An expired code answers <code>expired_token</code> once, on the first poll within five minutes of its expiry. That poll removes the code, so every later poll answers <code>invalid_grant</code>; a code nobody polls in those five minutes is gone and answers <code>invalid_grant</code> as well. A denial is reported once; the next poll finds nothing. <code>slow_down</code> means the device polled within five seconds of its last accepted poll. RFC 8628 asks the device to add five seconds to its interval, and the server refuses polls inside the window rather than widening it. <code>invalid_grant</code> is one answer for every code the client cannot redeem: a device presenting another client&#39;s code learns nothing about whether that code exists, and its poll neither consumes nor slows the legitimate device&#39;s flow.</p><h4 id="the-session-behind-the-token" tabindex="-1">The session behind the token <a class="header-anchor" href="#the-session-behind-the-token" aria-label="Permalink to &quot;The session behind the token&quot;">​</a></h4><p>Approving binds the browser session the person approved from. The device&#39;s tokens are issued under that session: <code>amr</code> and <code>acr</code> report how that session was established (<code>pwd</code>, or <code>ext</code> for a federated login made earlier at <code>/authorize</code>, plus <code>otp</code> and <code>urn:authup:mfa</code> after a second factor), the <code>id_token</code> carries that session&#39;s <code>sid</code> and, as <code>auth_time</code>, the instant that session was created (the login, not the approval), and the refresh token rotates like any other. A session that ended between the approval and the poll is not revived: the tokens are then issued under a new session created from the device&#39;s own request. Ending the approver&#39;s session ends the device&#39;s access too, whether through <code>DELETE /sessions/:id</code> or an RP-initiated logout with an <code>id_token_hint</code> of that session. Consent is recorded for a non-<code>builtIn</code> client, so the device application is listed on the account console&#39;s Applications page and can be revoked there.</p><h4 id="deployment-notes" tabindex="-1">Deployment notes <a class="header-anchor" href="#deployment-notes" aria-label="Permalink to &quot;Deployment notes&quot;">​</a></h4><p>The codes are cache entries with a ten minute lifetime, never database rows. A multi-replica deployment needs Redis for the flow: the request, the approval and the polls can land on different replicas. A cache flush ends every flow in flight; the poll then answers <code>invalid_grant</code> and the device starts over. Every approval and denial is recorded in the security event log (<code>authorize</code> with <code>data.reason: device</code>, <code>authorizeFailed</code> with <code>reason: denied</code>); the codes themselves never appear in an event.</p>`,133)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
