import{_ as s,o as i,c as a,a0 as t}from"./chunks/framework.BFac62L4.js";const k=JSON.parse('{"title":"Authorize resources with the authorization catalog","description":"","frontmatter":{},"headers":[],"relativePath":"sdks/javascript/access/authorization.md","filePath":"sdks/javascript/access/authorization.md"}'),n={name:"sdks/javascript/access/authorization.md"};function o(r,e,l,h,c,d){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="authorize-resources-with-the-authorization-catalog" tabindex="-1">Authorize resources with the authorization catalog <a class="header-anchor" href="#authorize-resources-with-the-authorization-catalog" aria-label="Permalink to &quot;Authorize resources with the authorization catalog&quot;">​</a></h1><p><code>GET /authorization</code> answers the identity-free authorization catalog (<code>AuthorizationCatalog</code>, exported by <code>@authup/access</code>): every permission definition with the policies bound to it, and each policy tree once. The grants an identity holds ride the introspection response as <code>permissions</code>, one entry per grant with its realm reach and its junction policy ids. Together with the identity that introspection names, that is the data a console or a resource server needs to evaluate Authup&#39;s decisions outside the Authup process. Reading the <code>name</code> of each entry alone is not enough for resource authorization.</p><h2 id="read-the-catalog-and-the-grants" tabindex="-1">Read the catalog and the grants <a class="header-anchor" href="#read-the-catalog-and-the-grants" aria-label="Permalink to &quot;Read the catalog and the grants&quot;">​</a></h2><p>The catalog endpoint is gated exactly like <code>GET /permissions</code>: the credential must hold <code>PERMISSION_READ</code>, <code>PERMISSION_UPDATE</code> or <code>PERMISSION_DELETE</code>, and any other authenticated credential is answered 403. A resource server therefore fetches it with its own client credential, never with the end user&#39;s bearer: the catalog is identity-free, so the user&#39;s grants say nothing about reading it, and one <code>client-permission</code> row binding <code>permission_read</code> to the resource server&#39;s client is all it needs.</p><p><strong>Bind that grant at <code>ownOrNull</code> or wider.</strong> The default realm scope of a junction is <code>own</code>, which does not reach a global row, and every permission is bound to the global <code>system.default</code> policy, so a credential granted the family and nothing else reaches no definition at all and is answered 403 rather than a document that denies everything.</p><p>What you get back is narrowed to your own realm reach, the way those two reads are narrowed. Reach removes the policy configuration rather than the entry: a definition outside your reach arrives with <code>policies: null</code> and denies, and a policy tree outside it arrives as a node no consumer can project, which drops the grants naming it. A foreign realm&#39;s definitions therefore still appear, carrying their <code>name</code>, <code>realm_id</code>, <code>client_id</code> and <code>decision_strategy</code> with no policies, so that an absent definition keeps its one meaning: your copy is older than the server&#39;s. Cache the answer per credential rather than per process, and give a resource server serving several realms a credential whose reach covers them.</p><p><strong>A 403 is not a catalog, and what to do with it depends on who you are.</strong> A console falls back to gating on the entry names alone, which is coarser than the catalog-backed evaluator: it ignores realm reach and junction policies, so a check a catalog-backed session denies passes there. That is deliberate for a console, whose gating is advisory (the server enforces every decision, and the user who lacks the permission family is the one whose custom roles a deny-all would blank the UI for), and it is the gating every console user had before the catalog existed. A resource server must never do it: it fails closed on a missing catalog, which is why it reads one with its own client credential rather than with the user&#39;s.</p><p>The grants and the identity come from the introspection you already run: <code>POST /token/introspect</code> for a bearer, <code>GET /sessions/@me/introspect</code> for a console session. <code>permissions</code> is the grant list; <code>sub</code>, <code>sub_kind</code>, <code>realm_id</code> and <code>realm_name</code> are the identity. The grant list is NOT narrowed by the token&#39;s <code>scope</code>: an active token reports the identity&#39;s full grant set, while the server withholds the identity policy data from a bearer that holds no <code>global</code> scope and denies it every <code>system.default</code>-bound permission. A resource server that honours scopes applies that check itself.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    BuiltInPolicyType,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    PolicyData,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    createAuthorizationEvaluator,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    isAuthorizationCatalogStaleError,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">} </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@authup/access&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// \`client\` authenticates with the resource server&#39;s own client credential,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// which holds permission_read; only the introspection speaks for the user.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> catalog</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.authorization.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> introspection</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.token.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">introspect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ token: accessToken }, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    authorizationHeader: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Bearer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, token: accessToken },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// An inactive token still names its subject, and it reports no grants at all.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Building an evaluator from it would authorize every definition that carries</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// no binding check, so refuse the credential before you build one.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">introspection.active) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;The access token is not active.&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> authorization</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createAuthorizationEvaluator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    catalog,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    grants: introspection.permissions </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    identity: {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        id: introspection.sub,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        type: introspection.sub_kind,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        realmId: introspection.realm_id,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        realmName: introspection.realm_name,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // a client subject is its own client; a user&#39;s own clientId is not a claim</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        clientId: introspection.sub_kind </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;client&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> introspection.sub </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p><code>createAuthorizationEvaluator</code> refuses an identity with no grant list, so the inactive case above cannot reach it by omission: an identity holding no grant passes an explicit empty array.</p><p>The response is <code>Cache-Control: private, no-cache</code>: keep it in your own process and refetch it when <code>createAuthorizationEvaluator</code> throws an error <code>isAuthorizationCatalogStaleError</code> recognizes, which means a grant names a definition or a policy the cached copy does not carry. Use that guard rather than <code>instanceof</code>: a tree resolving two copies of <code>@authup/access</code> breaks the class identity and turns the one recoverable state into an unrecoverable one. The grants come from a fresh introspection, so a definition created after the catalog was cached (an Authup upgrade adding permissions, a <code>POST /permissions</code>) or a junction row created since reaches you through that error and nothing else. An evaluator is per subject, because its grants and its identity are; the catalog behind it is shared. A revoked grant is visible on the next introspection.</p><h2 id="evaluate-one-resource" tabindex="-1">Evaluate one resource <a class="header-anchor" href="#evaluate-one-resource" aria-label="Permalink to &quot;Evaluate one resource&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> authorization.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">evaluate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;event_read&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // These name the PERMISSION definition, not the resource&#39;s realm.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    realmId: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    clientId: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    data: </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> PolicyData</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        [BuiltInPolicyType.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">REALM_MATCH</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]: event.realmId,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        [BuiltInPolicyType.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ATTRIBUTES</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]: event,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Resolves on allow; throws on denial.</span></span></code></pre></div><p>For an anonymous caller omit <code>identity</code> and <code>grants</code>. No identity data is injected and no grant is bound, so only a definition whose policies need no identity can pass (a <code>date</code> or <code>time</code> policy alone, or an empty definition layer), exactly as on the Authup server; a definition bound to <code>system.default</code> denies. Grants supplied without an identity are refused.</p><p><code>options.decisionStrategy</code> is forwarded; the policy include, exclude and pending options are refused.</p><p>Reach is enforced by the <code>permissionBinding</code> node in the permission&#39;s definition policies (<code>system.default</code> on every provisioned permission). A permission whose definition carries no binding check is unrestricted, grants included, so <code>allow</code> can also mean that the permission has no policy layer at all. Keep <code>core.permissionsDefaultPolicyAssignment</code> on for <code>realm_scope</code> to mean anything.</p><p>The <code>REALM_MATCH</code> key follows the same three-way rule the Authup server applies to its own entities. A resource that carries a realm column passes its value: the realm id, or <code>null</code> for a global row. A resource with no realm dimension passes no key at all, and reach neutral-passes for it. The trap is a resource that has a realm column whose value you forget to pass: reach then neutral-passes as if the resource were realm-less, so pass the column whenever it exists, <code>null</code> included. <code>own</code> admits the actor&#39;s realm only; <code>ownOrNull</code> also admits global rows; <code>any</code> admits every realm; <code>none</code> admits none. A realm-less actor cannot satisfy <code>own</code> or <code>ownOrNull</code>. Master-realm membership adds no bypass.</p><p><code>preEvaluate</code> and <code>preEvaluateOneOf</code> are the pre-gate for a request whose row is not loaded yet: a pending policy passes, and reach settles only when <code>REALM_MATCH</code> is present. <code>evaluate</code> requires every named permission, <code>evaluateOneOf</code> any.</p><h2 id="filter-collections-before-pagination" tabindex="-1">Filter collections before pagination <a class="header-anchor" href="#filter-collections-before-pagination" aria-label="Permalink to &quot;Filter collections before pagination&quot;">​</a></h2><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> compiled</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> authorization.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">compile</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;event_read&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">switch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (compiled.verdict) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    case</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;allow&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        break</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    case</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;deny&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // Return no rows and total: 0.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        break</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    case</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;conditional&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // AND compiled.condition into BOTH the row query and its count query,</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // using the corresponding rapiq adapter, BEFORE offset/limit.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        break</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    case</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;post&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;This collection query requires unsupported policy evaluation.&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>condition</code> is a rapiq condition over row attributes; grant reach targets the <code>realmId</code> column, or another column named with <code>compile({ name, realmAttributeName })</code> when your rows carry their realm elsewhere. Naming one is conservative about the rest of the grant: a junction policy is then left to <code>post</code> instead of being lowered, because a caller whose rows carry their realm elsewhere may not carry that policy&#39;s other fields either, and Authup cannot tell the two apart. If your rows do carry them and you want the pushdown, leave the option unset and map the realm column in your query adapter instead. Call <code>compile</code> without <code>realmMatch</code> or <code>attributes</code> data. A pending policy that cannot be lowered produces <code>post</code>, never an unrestricted query.</p><h2 id="catalog-and-grant-contract" tabindex="-1">Catalog and grant contract <a class="header-anchor" href="#catalog-and-grant-contract" aria-label="Permalink to &quot;Catalog and grant contract&quot;">​</a></h2><p>The catalog (<code>GET /authorization</code>):</p><ul><li><code>policies</code>: every policy tree a definition or a grant can name, keyed by the tree&#39;s id and present once. A node carries its <code>type</code>, its configuration keys, <code>invert</code> and, for a composite, its <code>children</code> inline. It is the output of that type&#39;s validator: no entity columns.</li><li><code>permissions</code>: one entry per definition with <code>name</code>, <code>realm_id</code>, <code>client_id</code>, <code>decision_strategy</code> and the definition&#39;s <code>policies</code> (ids), sorted by permission key.</li><li>Nullable fields are required. An empty <code>policies</code> list means no restriction at that layer. <code>policies</code> is <code>null</code> when the server could not project one of the definition&#39;s trees (a policy type the catalog does not carry, or a configuration its validator refuses): the definition exists, the evaluator denies it and drops every grant of it until the policy is fixed. Such a definition is carried rather than left out, so that a definition absent from the catalog can only mean a copy older than the definition.</li><li>A tree YOUR copy of <code>@authup/access</code> cannot project is read the same way: a policy type newer than that copy, or a configuration its validator refuses, denies the definitions that reference it and drops the grants that name it, and never takes the other permissions down with it. Upgrade <code>@authup/access</code> after the server, and expect the permissions using a newly added policy type to deny until you do.</li></ul><p>The grants (the introspection response&#39;s <code>permissions</code>):</p><ul><li>One entry per grant, so a name may repeat when the identity holds it through several junction rows.</li><li>For a user, only the grants that apply through the INTROSPECTED token&#39;s client are listed: a permission owned by another client is absent, and so is everything a role owned by another client carries. Build one evaluator per token rather than sharing one across tokens issued to different clients.</li><li><code>name</code>, <code>realm_id</code> and <code>client_id</code> name the definition. <code>realm_scope</code> is the grant&#39;s own reach, <code>own</code> when absent. <code>policies</code> are the ids of the junction policy trees, resolved against the catalog; empty means no junction policy.</li><li>A grant naming a definition the catalog lacks, or naming a policy the catalog lacks for a definition it carries, throws a stale error: the copy you hold predates the definition or the junction row, so refetch the catalog and build again. A grant naming a definition carried with <code>policies: null</code> is left out and denies, and its own policy ids are never resolved, so that case never reports stale. A grant whose junction policy contains a permission-binding check, or one your copy cannot project, is left out.</li></ul><p><code>createAuthorizationEvaluator</code> rejects missing fields, a definition referencing an undeclared policy id, a duplicate permission namespace, an identity that is not a <code>user</code> or a <code>client</code>, and grants supplied without an identity. It rebuilds the server&#39;s own binding model and runs the same aggregation and evaluators, which is what makes the decisions equal.</p><h2 id="upgrade-order" tabindex="-1">Upgrade order <a class="header-anchor" href="#upgrade-order" aria-label="Permalink to &quot;Upgrade order&quot;">​</a></h2><ol><li>Upgrade the Authup server to a release serving <code>GET /authorization</code> and reporting grants on introspection.</li><li>Upgrade <code>@authup/access</code> and, for typed access, <code>@authup/core-http-kit</code>.</li><li>Bind <code>permission_read</code> to the resource server&#39;s own client (one <code>client-permission</code> row) and fetch the catalog with that credential.</li><li>Replace name-only checks with <code>createAuthorizationEvaluator({ catalog, grants: introspection.permissions, identity })</code>.</li><li>A resource server must fail closed on a missing or malformed catalog and on an introspection without a grant list. Do not read the entries&#39; names alone there.</li></ol><h2 id="gating-a-public-client-with-the-batch-check" tabindex="-1">Gating a public client with the batch check <a class="header-anchor" href="#gating-a-public-client-with-the-batch-check" aria-label="Permalink to &quot;Gating a public client with the batch check&quot;">​</a></h2><p>The catalog is gated, and that gate is unsatisfiable for a <strong>public client</strong>: it holds no secret, so it can obtain no <code>client_credentials</code> token and has no credential of its own to be gated on. Making the catalog anonymous instead would publish every policy predicate to anyone who can reach the server, so a public client reads verdicts rather than configuration.</p><div class="language-typescript vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> permissions</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> client.authorization.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">check</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { realms: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;ownOrNull&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { authorizationHeader: { type: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;Bearer&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, token: accessToken } },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// [{ name: &#39;user_read&#39;, realms: [&#39;&lt;your realm id&gt;&#39;, null] }]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> evaluator</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> await</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createAuthorizationCheckEvaluator</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ permissions, identity });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">await</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> evaluator.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">preEvaluateOneOf</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ name: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;user_read&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span></code></pre></div><p><code>POST /authorization/check</code> needs an identity and no permission. Both body members are optional:</p><ul><li><code>names</code> restricts the check to a subset. Omit it and every definition is checked, which is the point: a client that names what it checks has to keep that list in step with its own UI, and a name missing from the request fails silently as a control that quietly disappears.</li><li><code>realms</code> is <code>own</code>, <code>ownOrNull</code> (the default) or an explicit list, with <code>null</code> for the global rows every realm shares. A symbolic selector resolves against your own identity. An explicit list has no realm key RESOLVED: each member is evaluated and echoed as you sent it, so the answer discloses no realm&#39;s existence and you match it against your own input. The one transformation is a duplicate drop, keeping the first occurrence and the order, since a repeated realm asks the same question twice. There is no <code>any</code>: name the realm you care about. At most 256 names and 4 realms per call.</li></ul><p>A permission that reaches none of the requested realms is <strong>absent</strong> from the answer, so absent means denied, and a check carrying a <code>realmMatch</code> for a realm you never asked about denies too. Ask about the realms your UI will ask about.</p><p>Two limits to hold on to:</p><ul><li><strong>It is an upper bound on what may be attempted, never an entitlement.</strong> It is a pre-gate, so a grant restricted by a junction policy that needs a resource row passes here and is still decided per row on the server. Use it for coarse gating of nav items, buttons and routes. A resource server deciding access to its own rows wants the catalog, which can evaluate those policies against the row and lower a collection query through <code>compile()</code>.</li><li><strong>A bare name means &quot;in at least one requested realm&quot;.</strong> A <code>realmScope: none</code> grant therefore does not pass, where a realm-less check would let it through.</li></ul>`,37)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
