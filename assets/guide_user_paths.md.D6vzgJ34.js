import{_ as s,o as a,c as i,a0 as n}from"./chunks/framework.CnTYizyl.js";const k=JSON.parse('{"title":"Paths (Folders for Users and Clients)","description":"","frontmatter":{},"headers":[],"relativePath":"guide/user/paths.md","filePath":"guide/user/paths.md"}'),t={name:"guide/user/paths.md"};function r(l,e,o,h,d,p){return a(),i("div",null,[...e[0]||(e[0]=[n(`<h1 id="paths-folders-for-users-and-clients" tabindex="-1">Paths (Folders for Users and Clients) <a class="header-anchor" href="#paths-folders-for-users-and-clients" aria-label="Permalink to &quot;Paths (Folders for Users and Clients)&quot;">​</a></h1><p>A <strong>path</strong> is a folder inside one realm that users and clients can be filed under, so that a large realm can be organized and navigated.</p><p>A path is therefore both:</p><ul><li>a node in a folder tree owned by exactly one realm; and</li><li>a label on the users and clients filed under it.</li></ul><p>A folder carries no authorization. Filing a user under <code>sales/berlin</code> grants nothing, withholds nothing, and moving a row between folders changes nothing about what it may do. Organization and authorization are deliberately kept apart: roles, permissions and policies decide access, folders decide where a row lives.</p><h2 id="what-a-path-is-not" tabindex="-1">What a path is not <a class="header-anchor" href="#what-a-path-is-not" aria-label="Permalink to &quot;What a path is not&quot;">​</a></h2><p>Filing a user or a client under a folder does not:</p><ul><li>grant or withhold a permission, a role or a scope;</li><li>affect a login, a token, a consent or a session;</li><li>by itself restrict who may read, update or delete the row;</li><li>make the folder a tenant. The realm is the isolation boundary: it owns the signing keys, the discovery document and the system clients. A folder is a label inside one realm and never spans realms.</li></ul><p>Delegating administration by folder is possible, but only as an explicit policy an administrator writes. The folder never does it on its own. See <a href="#delegating-a-subtree">Delegating a subtree</a>.</p><h2 id="how-the-tree-works" tabindex="-1">How the tree works <a class="header-anchor" href="#how-the-tree-works" aria-label="Permalink to &quot;How the tree works&quot;">​</a></h2><p>A folder has a parent and a single name segment. The server derives the full path from the parent chain and stores it on the folder:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sales          name: sales        parent: (none)   path: sales</span></span>
<span class="line"><span>  berlin       name: berlin       parent: sales    path: sales/berlin</span></span>
<span class="line"><span>  hamburg      name: hamburg      parent: sales    path: sales/hamburg</span></span>
<span class="line"><span>engineering    name: engineering  parent: (none)   path: engineering</span></span></code></pre></div><p>The full path is never accepted from a caller. It is recomputed whenever a folder is created, renamed or moved, and it is unique inside the realm: two folders under one parent cannot carry the same name, while two realms may both hold a <code>sales/berlin</code>.</p><ul><li><strong>Names</strong> use the same character set as every other Authup name (<code>a</code> to <code>z</code>, <code>0</code> to <code>9</code>, <code>-</code>, <code>_</code>, <code>.</code>) and are trimmed and lowercased on write, so <code>Berlin</code> is stored as <code>berlin</code>. A segment holds at most 128 characters, a full path at most 255, and the tree is at most 15 levels deep.</li><li><strong>Empty folders are allowed.</strong> Build the structure first and file rows into it afterwards.</li><li><strong>Renaming or moving</strong> a folder recomputes its own path and rewrites every descendant path in one transaction. The users and clients filed under those folders keep their reference and are not written at all: renaming <code>sales</code> to <code>marketing</code> turns <code>sales/berlin</code> into <code>marketing/berlin</code> and everyone filed there follows. A folder cannot be moved under itself or under one of its own descendants.</li><li><strong>Deleting a folder deletes its descendants and unfiles their occupants</strong> in the same statement. No user and no client is ever deleted with a folder: the folder reference is cleared and the rows become unfiled. This is never refused and needs no force flag. Before it sends the request the admin console reads the subtree and names three numbers in the confirmation: how many subfolders are deleted, and how many users and how many clients are unfiled. When one of those reads is refused, or the subtree is larger than the console can resolve, the confirmation says the counts are unavailable and describes the effect instead.</li></ul><h2 id="filing-a-user-or-a-client" tabindex="-1">Filing a user or a client <a class="header-anchor" href="#filing-a-user-or-a-client" aria-label="Permalink to &quot;Filing a user or a client&quot;">​</a></h2><p>A user and a client each carry one optional folder reference. No reference means <strong>unfiled</strong>, and unfiled is the default: nothing is filed automatically, and an upgrade migrates no row into a folder.</p><p>Unlike the realm, which is fixed at creation, the folder is mutable: a row is refiled by pointing it at another folder. The folder must belong to the same realm as the row, otherwise the write is rejected.</p><p>A user cannot refile itself. The folder reference is on the self-management denylist for both users and clients, so it is an administrative field even on the account console, where the signed-in user sees their own folder as a read-only value.</p><h2 id="managing-folders" tabindex="-1">Managing folders <a class="header-anchor" href="#managing-folders" aria-label="Permalink to &quot;Managing folders&quot;">​</a></h2><p>Folders are administered through the <code>path_read</code>, <code>path_create</code>, <code>path_update</code> and <code>path_delete</code> permissions, over the routes <code>/paths</code> and <code>/realms/:realmId/paths</code>. The global <code>admin</code> role reaches every realm. A <code>realm_admin</code> reads the folders of its own realm and of no other, and creates, updates and deletes only inside its own realm.</p><p>Reading the folder of a user or a client is not gated on <code>path_read</code>. A folder row holds organizational metadata only (its name, its path, a display name and a description), so a reader who may see the row may see where it is filed: the folder column in the admin console&#39;s user list renders for every reader of that list, and the account console shows a user their own folder without granting them anything. The folder <strong>collection</strong> is gated: listing the realm&#39;s folders requires the read permission, which is why the folder tree that scopes the users and clients lists is shown only to a reader holding it.</p><p>To manage folders in the admin console:</p><ol><li>Select the target realm.</li><li>Open <strong>Paths</strong> in the sidebar and build the tree there.</li><li>On the users and clients pages, pick a folder in the tree beside the table to scope the list to that folder and everything below it. The chosen folder is written into the address, so a scoped list can be shared as a link and survives a reload; <strong>All paths</strong> clears it. The search box is independent of that scope: it searches names and display names, and the two narrow the list together. A subtree with more folders than the console can carry in one request is not scoped at all: the page then lists every row and says why, rather than showing a silently shortened list.</li><li>On a user or client form, choose the folder the row belongs to.</li></ol><h2 id="federated-users" tabindex="-1">Federated users <a class="header-anchor" href="#federated-users" aria-label="Permalink to &quot;Federated users&quot;">​</a></h2><p>A user created by an <a href="./identity-providers.html">identity provider</a> is filed under <code>sources/&lt;provider name&gt;</code> on first login, and the folder chain is created if it does not exist yet. An attribute mapping that targets the folder wins over that default.</p><p>That <strong>default</strong> applies at creation only. A later login never refiles an existing user, so an administrator who moves a federated user elsewhere keeps that placement, and a folder that cannot be created leaves the user unfiled rather than failing the login.</p><p>An <strong>attribute mapping</strong> that targets the folder behaves like every other mapped attribute and is re-applied on <strong>every</strong> login, so it overwrites a manual refile. It must supply the folder&#39;s <strong>UUID</strong>: nothing resolves a path such as <code>sales/berlin</code> to a folder there. The folder must belong to the user&#39;s realm, and a mapping naming another realm&#39;s folder is refused, which fails that provider&#39;s logins until the mapping is corrected.</p><p>Nothing is reserved about the <code>sources</code> prefix: it is a convention, and an ordinary folder may live next to it.</p><h2 id="declaring-folders-in-a-provisioning-file" tabindex="-1">Declaring folders in a provisioning file <a class="header-anchor" href="#declaring-folders-in-a-provisioning-file" aria-label="Permalink to &quot;Declaring folders in a provisioning file&quot;">​</a></h2><p>A realm entry declares its folders by full path, and a user or client entry references one the same way. Nothing has to be declared before it is used: <code>sales/berlin</code> creates <code>sales</code> first when it is missing, and a user that references a folder no entry declares creates that folder too. Declaring a folder is how it gets a display name or a description.</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">realms</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">attributes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">acme</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    relations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      paths</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">attributes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">sales/berlin</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            displayName</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Berlin</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">attributes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">engineering</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      users</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">attributes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">alice</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            password</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">replace-with-a-strong-secret</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          relations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">sales/berlin</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      clients</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">attributes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">acme-app</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            authMethod</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">none</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          relations</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">            path</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">engineering</span></span></code></pre></div><p>Folders are synchronized before the realm&#39;s clients and users, so a folder declared in the same file is available to reference. See <a href="./../deployment/provisioning.html">Provisioning</a> for the file format, the sync strategies and the wildcard realm entry that seeds one folder set into every realm.</p><h2 id="delegating-a-subtree" tabindex="-1">Delegating a subtree <a class="header-anchor" href="#delegating-a-subtree" aria-label="Permalink to &quot;Delegating a subtree&quot;">​</a></h2><p>A folder decides nothing by itself, but an administrator can write a policy that reads it. Attach an <code>attributes</code><a href="./permissions-and-policies.html">policy</a> to the grant that gives a role its <code>user_read</code> / <code>user_update</code> / <code>user_delete</code> permissions, and restrict it to a set of folders:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">policies</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">attributes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">sales-folders</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">attributes</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    extraAttributes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      query</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        pathId</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          $in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">0f1c5d3e-1b7a-4c1f-9e11-2f6a7d0c4b11</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            - </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">7a2e9b64-5d30-4f8c-bf02-1c9d3e5a8042</span></span></code></pre></div><p>A user or a client carries its folder reference, not the folder&#39;s path, so a policy over those rows names folder ids. A subtree is expressed over the folder rows themselves: a policy on a <code>path_read</code> or <code>path_update</code> grant with <code>{ path: { $startsWith: &#39;sales/&#39; } }</code> reaches every folder below <code>sales</code>.</p><p>Two limits are worth knowing before relying on this:</p><ul><li><strong>Only a global administrator can author it.</strong> Attaching a policy to a grant requires an unrestricted, policy-free grant of that permission. A folder-scoped administrator can pass on its own policy unchanged and nothing narrower, so a <code>sales</code> administrator cannot mint a <code>sales/emea</code> one. Folder delegation cannot be sub-delegated.</li><li><strong>Never use <code>$regex</code>.</strong> It is not portable across the databases Authup supports and fails the query outright on SQLite. Use <code>$in</code> over folder ids, or <code>$startsWith</code> over a folder path.</li></ul><h2 id="when-you-do-not-need-paths" tabindex="-1">When you do not need paths <a class="header-anchor" href="#when-you-do-not-need-paths" aria-label="Permalink to &quot;When you do not need paths&quot;">​</a></h2><p>Most deployments do not need folders. A realm already separates tenants, and the admin console searches users and clients by name and display name without one. Reach for folders when a single realm holds enough rows that browsing it needs structure, when the provenance of an account is worth recording, or when a set of rows should be addressable as a group for an administrator who wants to write the delegation policy above.</p>`,40)])])}const g=s(t,[["render",r]]);export{k as __pageData,g as default};
