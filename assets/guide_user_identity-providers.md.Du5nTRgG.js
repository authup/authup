import{_ as t,o,c as a,a0 as i}from"./chunks/framework.ZNXClGez.js";const u=JSON.parse('{"title":"Identity Providers","description":"","frontmatter":{},"headers":[],"relativePath":"guide/user/identity-providers.md","filePath":"guide/user/identity-providers.md"}'),n={name:"guide/user/identity-providers.md"};function r(s,e,d,h,c,l){return o(),a("div",null,[...e[0]||(e[0]=[i('<h1 id="identity-providers" tabindex="-1">Identity Providers <a class="header-anchor" href="#identity-providers" aria-label="Permalink to &quot;Identity Providers&quot;">​</a></h1><p>An identity provider lets people sign in to Authup with an account they already have somewhere else: another Authup instance, Keycloak, Authentik, GitHub, Google, an LDAP directory, or any OAuth2 / OpenID Connect server.</p><p>Providers are configured per realm in the admin console under <strong>Identity Providers</strong>. Each one appears as a button on that realm&#39;s login page.</p><h2 id="what-a-login-looks-like" tabindex="-1">What a login looks like <a class="header-anchor" href="#what-a-login-looks-like" aria-label="Permalink to &quot;What a login looks like&quot;">​</a></h2><ol><li>The person opens an application, which sends them to Authup&#39;s hosted <code>/authorize</code> page.</li><li>They pick a provider instead of typing a password.</li><li>Authup sends them to that provider, where they authenticate.</li><li>The provider sends them back to Authup, which establishes the session and returns them to the same <code>/authorize</code> page they started on.</li><li>That page finishes the login the way it finishes any other one: it applies <code>prompt</code> and <code>max_age</code>, shows the consent screen for an application that needs one, and only then issues the application&#39;s authorization code.</li></ol><p>Step 5 is worth knowing about, because it means a federated login is not a shortcut around the login page. The application receives its code from the same place an ordinary login delivers it, and the person sees the same consent screen.</p><h2 id="accounts" tabindex="-1">Accounts <a class="header-anchor" href="#accounts" aria-label="Permalink to &quot;Accounts&quot;">​</a></h2><p><strong>A first login creates a new user.</strong> Authup does not match an external identity to an existing account by email address, since anyone who can control an email claim at the provider could otherwise take over an account.</p><p><strong>An existing account is linked by its owner.</strong> A signed-in person links a provider from the account console under <strong>Connected accounts</strong>. From then on, signing in through that provider signs them in to that account.</p><p>An administrator can see and remove links (a user&#39;s <strong>Identity provider accounts</strong> tab) but cannot create one, for the same reason.</p><h2 id="multi-factor-authentication" tabindex="-1">Multi-factor authentication <a class="header-anchor" href="#multi-factor-authentication" aria-label="Permalink to &quot;Multi-factor authentication&quot;">​</a></h2><p><strong>The provider is the authentication authority, so Authup does not ask for a second factor of its own on top of a federated login.</strong> Configure and enforce MFA at the provider. <code>mfaRequired</code> does not force local enrollment on people who sign in this way either.</p><p>This is the same default Keycloak and Authentik ship, and it follows from how accounts are linked: a person reaches an existing Authup account through a provider only if they linked it while signed in, so the route was their choice.</p><p>There is one exception. An application can request a proof explicitly by sending <code>acr_values=urn:authup:mfa</code> with its authorization request. Then Authup challenges a factor the person holds before issuing that application&#39;s code, whichever way they signed in.</p><p>The tokens say what happened: <code>amr</code> is <code>[&quot;ext&quot;]</code> for a federated login, and carries <code>&quot;otp&quot;</code> with <code>acr: urn:authup:mfa</code> when a factor was verified. A federated login that verified no local factor carries no <code>acr</code> at all, because Authup checked no credential of its own.</p><h3 id="verifying-what-the-provider-did" tabindex="-1">Verifying what the provider did <a class="header-anchor" href="#verifying-what-the-provider-did" aria-label="Permalink to &quot;Verifying what the provider did&quot;">​</a></h3><p>&quot;MFA is enforced at the provider&quot; is a statement you make, and by default Authup takes it on faith: an upstream that lets someone in with a password alone is accepted, and nothing notices. Two optional fields on an OAuth2/OIDC provider turn the assumption into a check on the claims that provider&#39;s own token carries.</p><ul><li><strong>Required amr claim(s)</strong> accepts the login when the provider&#39;s <code>id_token</code> carries any one of the listed values in its <code>amr</code> claim.</li><li><strong>Required acr value(s)</strong> accepts it when the token&#39;s <code>acr</code> is one of the listed values. Authup also sends these as <code>acr_values</code> on the authorize request, so the provider is asked to step up rather than only observed.</li></ul><p>Both take a comma- or space-separated list and are empty by default, which is the unchecked behaviour above. The vocabularies belong to the provider, so the values do too: Keycloak and Authentik commonly answer <code>amr: [&quot;mfa&quot;]</code> or an <code>acr</code> level of your own configuring, Entra ID answers <code>amr: [&quot;pwd&quot;, &quot;mfa&quot;]</code>. Read the provider&#39;s documentation, or sign in once and look at the <code>amr</code> and <code>acr</code> in its token. Another Authup upstream answers <code>acr: urn:authup:mfa</code> once a second factor was verified, so <code>urn:authup:mfa</code> is the value to require there.</p><p>Set either one and the check fails closed. A login is refused when the claim is missing, when it does not match, and when the provider returns no <code>id_token</code> at all - which is what a provider configured as plain <code>oauth2</code>, or an OIDC one whose scope no longer asks for <code>openid</code>, will do. The person is returned to the login page; the reason is in the Authup server log, because the login page deliberately says nothing about why a provider was refused.</p><p>The check also covers connecting an account from the account console, since that admits an external identity to an account that already exists.</p><p>One caveat, and it applies to every provider field rather than just these two: an update that omits a field clears it. A script that rotates the client secret by sending only the fields it cares about will drop the allow-lists with everything else it left out, and logins go back to being unchecked. Send the whole provider, which is what the admin console does.</p><p>See <a href="./../deployment/configuration-server-core-mfa.html">MFA configuration</a> for the server settings.</p><h2 id="configuring-a-provider" tabindex="-1">Configuring a provider <a class="header-anchor" href="#configuring-a-provider" aria-label="Permalink to &quot;Configuring a provider&quot;">​</a></h2><p>For an OAuth2 or OpenID Connect provider you need, from that provider:</p><ul><li>the authorize and token URLs (an OpenID provider usually publishes them in its discovery document),</li><li>a client id and client secret registered there for Authup,</li><li>the scopes to request.</li></ul><p>And at the provider, Authup&#39;s callback URL must be registered as a valid redirect URI:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&lt;publicUrl&gt;/identity-providers/&lt;provider-id&gt;/authorize-in</span></span></code></pre></div><p>The provider id is shown on the provider&#39;s detail page in the admin console.</p><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h2><p><strong>&quot;The login request is unknown or expired.&quot;</strong> The browser did not present the cookie that was set when the login started, or it did and the login had already been completed or had expired (five minutes). A browser that blocks all cookies cannot sign in through a provider at all: the cookie is what ties the login to the browser that began it, so that a link someone else opens cannot sign them into the wrong account. Starting the login again is the fix.</p><p><strong>Two federated logins started in the same browser at once.</strong> Only the most recent one can be completed; the other returns to the login page and has to be started again. The pending login is held in a single cookie per browser.</p><p><strong>The person lands back on the login page with no message.</strong> The application&#39;s authorization request no longer verifies: the client was deactivated or deleted, its <code>redirect_uri</code> no longer matches a registered pattern, or a scope was removed. The login page re-renders the same request, so it states the reason itself.</p><p><strong>&quot;The identity provider is not available.&quot;</strong> The provider was disabled while the person was away at it.</p><p><strong>Access was denied.</strong> Either the person&#39;s Authup user is inactive, or the application carries an access policy that the person does not satisfy.</p><p><strong>The provider says the redirect URI is invalid.</strong> The callback URL above is not registered at the provider, or <code>publicUrl</code> does not match the address the browser actually reaches Authup on.</p>',36)])])}const g=t(n,[["render",r]]);export{u as __pageData,g as default};
